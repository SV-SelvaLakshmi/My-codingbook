//Write a program to accept an input real value with decimals and print it as rounded off to 2 decimals
#include<stdio.h>
#include<math.h>
int main(){
   double user;
   scanf("%lf",&user);
   printf("%.2lf",user);

    return 0;
}
